{
  "name": "speedtest",
  "version": "1.0.0",
  "description": "A simple network speed test website (download, upload, ping)",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
